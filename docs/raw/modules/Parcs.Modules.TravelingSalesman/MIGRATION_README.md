# Migration Model для TSP модуля

## Огляд

**Migration Model** - це просунутий підхід до паралельного генетичного алгоритму, де воркери обмінюються кращими особями між собою через певні інтервали.

## Переваги Migration Model

### 🚀 **Покращена збіжність**
- **Обмін генами** між різними популяціями
- **Різноманітність** пошуку рішень
- **Швидше знаходження** глобального оптимуму

### 🔄 **Різні стратегії міграції**
- **BestIndividuals** - найкращі особини
- **RandomIndividuals** - випадкові особини  
- **DiverseIndividuals** - різноманітні особини
- **TournamentSelection** - турнірний вибір

### ⚡ **Гнучкість налаштування**
- **MigrationSize** - кількість особин для міграції
- **MigrationInterval** - інтервал міграції
- **EnableMigration** - увімкнення/вимкнення міграції

## Використання

### 1. **Налаштування міграції**

```csharp
var options = new ModuleOptions
{
    // Основні параметри
    PopulationSize = 1000,
    Generations = 100,
    PointsNumber = 4,
    
    // Налаштування міграції
    EnableMigration = true,
    MigrationType = MigrationType.BestIndividuals,
    MigrationSize = 5,
    MigrationInterval = 10
};
```

### 2. **Запуск з міграцією**

```csharp
// Використовуємо спеціальний модуль
await point.ExecuteClassAsync<ParallelWorkerModuleWithMigration>();
```

### 3. **Типи міграції**

#### **BestIndividuals**
```csharp
MigrationType = MigrationType.BestIndividuals
// Мігрують найкращі особини з кожної популяції
```

#### **RandomIndividuals**
```csharp
MigrationType = MigrationType.RandomIndividuals
// Мігрують випадкові особини для різноманітності
```

#### **DiverseIndividuals**
```csharp
MigrationType = MigrationType.DiverseIndividuals
// Мігрують особини з різних частин популяції
```

#### **TournamentSelection**
```csharp
MigrationType = MigrationType.TournamentSelection
// Турнірний вибір для міграції
```

## Архітектура

### **Основні компоненти**

1. **MigrationManager** - управляє процесом міграції
2. **GeneticAlgorithm** - підтримує міграцію
3. **ParallelMainModuleWithMigration** - основний модуль
4. **ParallelWorkerModuleWithMigration** - worker модуль

### **Процес міграції**

```
Покоління 1-9: Еволюція без міграції
Покоління 10: Міграція кращих особин
Покоління 11-19: Еволюція з новими генами
Покоління 20: Міграція кращих особин
...
```

### **Алгоритм міграції**

1. **Вибір особин** для міграції з кожної популяції
2. **Обмін** між воркерами
3. **Впровадження** нових генів
4. **Продовження** еволюції

## Приклад роботи

### **Без міграції (Island Model)**
```
Worker 1: [A1, A2, A3, A4, A5] - еволюціонує незалежно
Worker 2: [B1, B2, B3, B4, B5] - еволюціонує незалежно
Worker 3: [C1, C2, C3, C4, C5] - еволюціонує незалежно
Worker 4: [D1, D2, D3, D4, D5] - еволюціонує незалежно
```

### **З міграцією (Migration Model)**
```
Покоління 10:
Worker 1: [A1, A2, A3, A4, A5] + B1 + C1
Worker 2: [B1, B2, B3, B4, B5] + A1 + D1  
Worker 3: [C1, C2, C3, C4, C5] + A2 + B2
Worker 4: [D1, D2, D3, D4, D5] + B2 + C2
```

## Налаштування продуктивності

### **Рекомендовані параметри**

| Кількість міст | PopulationSize | MigrationSize | MigrationInterval |
|----------------|----------------|---------------|-------------------|
| 100-500        | 200-500        | 3-5           | 5-10             |
| 500-1000       | 300-800        | 5-8           | 10-15            |
| 1000+          | 500-1000       | 8-12          | 15-25            |

### **Оптимізація пам'яті**

```csharp
// Для великих задач
options.MigrationSize = Math.Min(options.MigrationSize, options.PopulationSize / 20);
options.MigrationInterval = Math.Max(options.MigrationInterval, 5);
```

## Тестування

### **Запуск тесту міграції**

```csharp
// В TestTSPModule
MigrationTest.TestMigrationModel();
```

### **Перевірка роботи**

1. **Логування** міграцій
2. **Порівняння** результатів з/без міграції
3. **Аналіз** збіжності

## Порівняння з Island Model

| Аспект           | Island Model | Migration Model |
|------------------|--------------|-----------------|
| **Простота**     | ✅ Висока     | ⚠️ Середня      |
| **Збіжність**    | ⚠️ Середня    | ✅ Висока        |
| **Пам'ять**      | ✅ Низька      | ⚠️ Середня      |
| **Швидкість**    | ✅ Висока      | ⚠️ Середня      |
| **Якість**       | ⚠️ Середня    | ✅ Висока        |

## Висновок

**Migration Model** дає кращі результати за рахунок обміну генами між воркерами, але потребує більше ресурсів та складнішої реалізації.

**Рекомендація**: Використовувати для задач, де важлива якість рішення, а не швидкість виконання. 